import { ExtendedItem } from "../ExtendedItem";
import { SelectItemOptions } from "../ItemOptions";

export class querySelector extends ExtendedItem {
	protected options: SelectItemOptions;

	protected addEventListeners(): void {

	}

	protected createDom(): void {

		// const options = ``;
		// for(const option of this.options.options) {

		// }

		// this.inputWrapper.innerHTML = `
		// 	<div class="${BASE_CLASS}-select">
		// 		<div class="${BASE_CLASS}-select__input">
		// 			<p>Test</p>
		// 		</div>
		// 		<div class="${BASE_CLASS}-select__options">
		// 			<div>Option 1</div>
		// 		</div>
		// 	</div>
		// `;

	}
}